{% extends '_layout' %}

{% block content %}
  {% include '_components/search/index.twig' %}
  {% include '_components/grid/index.twig' with {
    cards: craft.entries.section('crawls')
  } %}

  {% if entry is defined %}
    {% include '_components/modal/index.twig' with {
      show: true,
      data: entry
    } %}
  {% endif %}
{% endblock %}