{% extends '_layout' %}

{% block content %}
  {% include '_components/search/index.twig' %}

  <div class="listing">
    <section class="listing__grid grid">
      {% for i in 0..17 %}
        {% include '_components/card/index.twig' %}
      {% endfor %}
    </section>

    <a class="listing__load-more button button--secondary" href="#">Load more</a>
  </div>

  {% include '_components/modal/index.twig' %}
{% endblock %}